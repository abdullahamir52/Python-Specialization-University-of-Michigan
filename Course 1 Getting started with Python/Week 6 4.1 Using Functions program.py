# =============================================================================
# Program 1
# =============================================================================

def thing():
# def is the keyword that we use to define a function
# function is like a variable. Except it holds code, instead of a value
# If a function is called/invoked, then the code inside the function is executed.
# Otherwise, the function's code are not executed, just stored 
    print('Hello')
    print('Fun')
thing()
print('Zip')
thing()

# =============================================================================
# Program 2 
# =============================================================================
big = max('Hello world')
# max() and min() are built-in functions
# within the bracket of max(), we have parameter= argument or inputs
print(big)
tiny = min('Abdullah')
print(tiny)